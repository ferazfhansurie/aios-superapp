/** Map a file extension to a Monaco language id. Defaults to plaintext.
 *  Thin re-export — the canonical ext → language table lives in fileKinds.ts. */
export { languageForPath } from "./fileKinds";
